# 도형 — 넓이 함수를 담은 재사용 모듈
# 이 파일은 다른 프로그램이 가져다 쓸 값들을 정의합니다.
# 실행: nme r area.ko (이 모듈은 직접 실행하지 않습니다)

def rect(너비, 높이):
    return 너비 * 높이


def circle(반지름):
    return 3.14 * 반지름 * 반지름


원주율 = "원주율은 약 3.14"
