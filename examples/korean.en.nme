# NME keywords and variable names can all be written in English.
# Run: nme r korean.en.nme
use random

friend = "Ada"
pick = random_pick(["cat"])

if friend:
    2 times: say f"Give {friend} a {pick}!"
