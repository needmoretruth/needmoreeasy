# NME 0.0.1-beta.80

beta.80에서는 생성된 네이티브 C를 일반적인 경고를 오류로 처리하는 설정에서도
읽기 쉽고 깨끗하게 사용할 수 있습니다.

- GCC와 Clang에서는 사용하지 않는 런타임 도우미를 명시적으로 표시하고,
  MSVC에서는 그에 맞는 경고 억제를 적용합니다.
- 생성된 도우미 매크로 `NME_UNUSED`를 예약하여 사용자의 식별자가 C 전처리기에
  의해 바뀌지 않게 합니다.
