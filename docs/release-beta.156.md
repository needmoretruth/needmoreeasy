# NME 0.0.1-beta.156

Beta156 extends Python control-flow diagnostics to one-line function and class
suites. `return`, `break`, and `continue` now receive the stable diagnostic for
their actual context instead of inheriting an outer function or loop.
