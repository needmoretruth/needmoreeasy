# NME 0.0.1-beta.148

Beta148 adds shared bilingual `E0115` for `break`, `continue`, and `return`
inside Python `except*` blocks. Nested function bodies and control flow after
the handler suite remain unchanged.
