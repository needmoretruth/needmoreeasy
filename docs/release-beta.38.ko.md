# NME 0.0.1-beta.38

beta.38에서는 네이티브 명령의 폴더 처리를 다른 CLI 명령과 맞춥니다.

- `nme native <폴더>`를 파일 읽기 실패 E9007이 아니라 `폴더는 프로그램이
  아닙니다` E9014로 표시합니다.
- 영어 전용 경로와 한국어 우선 한영 경로가 같은 안정적인 진단과 실행 가능한
  폴더 안내를 사용하게 합니다.
- 두 네이티브 명령 언어의 회귀 검사를 추가했습니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
