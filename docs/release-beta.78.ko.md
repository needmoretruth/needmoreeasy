# NME 0.0.1-beta.78

beta.78에서는 원본 줄기가 `.c`로 끝나는 프로그램도 기본 네이티브 빌드를
할 수 있게 합니다.

- `count.c.nme` 원본은 `count.c`를 기본 실행 파일로, `count.c.c`를 생성 C
  소스로 사용할 수 있습니다.
- 명시적인 `-o count.c` 충돌 방지 검사는 유지하여 요청한 실행 파일 경로가
  생성 C 결과물을 덮어쓰지 않게 합니다.
