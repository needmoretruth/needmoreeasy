# NME 0.0.1-beta.43

beta.43에서는 네이티브 백엔드의 고정 용량 문자열 런타임을 안전하게 합니다.

- 검사하지 않는 `strcpy`/`strcat` 대신 안전성을 검사하는 복사와 이어붙이기를
  사용합니다.
- 8191 UTF-8바이트보다 큰 저장·이어붙이기 값은 생성된 C 버퍼를 넘치게 하지
  않고 영어·한국어 런타임 오류로 중단합니다.
- 네이티브 제한을 문서화하고 제한 없는 텍스트에는 CPython 경로를 안내합니다.
