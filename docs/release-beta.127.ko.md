# NME 0.0.1-beta.127

beta127에서는 네이티브 한 줄 NME `else if`/`else` 출력 본문을 여섯 표면에서
확인하는 회귀 검사를 추가합니다.

- 영어·한국어 문장형·초급·고급 문법을 모두 실행합니다.
- 인라인 `else if`와 인라인 `else` 결과를 모두 실행합니다.
- 올바른 Python 형태인 고급 한국어 조건은 Python 우선 경로를 유지합니다.
