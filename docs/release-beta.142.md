# NME 0.0.1-beta.142

Beta142 adds shared bilingual diagnostics for Python `yield` and `await`
context errors, and makes nested class scopes stop inheriting an outer
function context for `return` and `yield` checks.
