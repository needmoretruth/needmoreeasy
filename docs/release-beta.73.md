# NME 0.0.1-beta.73

Beta.73 keeps the native-backend documentation aligned with the implementation.

- Clarify that `nme native` compiles a restricted NME subset to C, while
  `nme compile` uses the separate Python/Nuitka path.
- Add regression coverage for blank lines and comments between native function
  headers and bodies.
