# NME 0.0.1-beta.91

beta.91에서는 한국어 도구 사용 안내를 한국어 CLI 경로에 맞춥니다.

- 영어 명령과 짧은 별칭을 함께 유지하면서 `nme 검사`와 `nme 빌드`를 가르칩니다.
- CLI가 실제로 출력하는 한국어 우선 한영 `E0102` 진단을 그대로 보여 줍니다.
- 한국어 변환 프로그램 연습에서는 `nme 검사`와 `nme 실행`을 사용합니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
