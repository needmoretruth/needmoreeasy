# NME 0.0.1-beta.30

beta.30에서는 네이티브 빌드 진단 안내를 명확하게 했습니다.

- E9010·E9011이 두 백엔드 경로를 모두 설명합니다. `nme 컴파일`은 Nuitka를,
  `nme 네이티브`(영어 `nme native`)는 `cc`나 `clang` 같은 시스템 C 컴파일러를 사용합니다.
- 두 도구 체인의 한영 공개 코드 조회 회귀 검사를 추가했습니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
