# NME 0.0.1-beta.75

Beta.75 updates the README compiler descriptions to match the implemented
native backend.

- Describe the v0 NME-native C backend and its restricted-subset roadmap in
  both compiler entry points.
- Distinguish the CPython-compatible path, optional Nuitka compilation, and
  the separate `nme native` command.
