# NME 0.0.1-beta.91

Beta.91 aligns the Korean tool-use guides with the Korean CLI path.

- Teach `nme 검사` and `nme 빌드` while retaining the English commands and
  short aliases.
- Show the Korean-first bilingual `E0102` diagnostic exactly as the CLI emits
  it.
- Use `nme 검사` and `nme 실행` for the Korean converted-program exercise.

Language semantics and generated Python behavior are unchanged.
