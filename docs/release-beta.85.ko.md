# NME 0.0.1-beta.85

beta.85에서는 Python 패키지 안내를 실제 패키지 설치 흐름과 맞춥니다.

- 초보자에게 운영체제별 pip 문법을 바로 안내하는 대신 `nme 설치 requests`와
  같은 뜻의 `nme install requests`를 가르칩니다.
- 래퍼가 일반 Python 명령을 고르고 한 번에 패키지 하나를 설치하며 pip 실패 때
  E9025를 보여 준다는 점을 설명합니다.
- 영어와 한국어 패키지 학습 경로를 맞춥니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
