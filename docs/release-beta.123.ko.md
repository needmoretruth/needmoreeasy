# NME 0.0.1-beta.123

beta.123에서는 제한된 네이티브 백엔드에 조건 블록과 분기 사슬의 한 줄 NME
출력 본문을 확장합니다.

- `then`/`그러면` 뒤가 `say`/`show`/`말해`이면 `if`/`while` 본문과
  `else if`/`else` 본문을 하강합니다.
- 인라인 분기도 들여쓴 블록과 같은 네이티브 흐름 추적을 사용합니다.
- Python 인라인 본문과 인라인 값 변경은 문서에 정한 네이티브 부분집합 밖으로
  계속 명확하게 거부합니다.
