# NME 0.0.1-beta.27

Beta.27 fixes a beginner-template control-flow trap.

- Bound the English beginner loop in both example-template twins with
  `while value < 3:` so the following condition can actually run.
- Add a documentation parity guard for the bounded English and Korean loops.

Language semantics and generated Python behavior are unchanged.
