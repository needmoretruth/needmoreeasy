# NME 0.0.1-beta.118

beta.118에서는 괄호 안의 한국어 비교 끝말을 쓰는 `elif` 조건의 회귀 검사를
강화합니다.

- `아니면 만약에 (점수가 4보다 작으면)` 같은 분기를 코어에서 정확히 하강하고
  네이티브로 실행하는지 확인합니다.
- `if`와 `while`에서 사용하는 공통 조건 범위 경로를 유지하며 언어별 구현을
  따로 만들지 않습니다.
