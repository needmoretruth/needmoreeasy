# NME 0.0.1-beta.124

Beta124 synchronizes the native backend design memo with the one-line NME
control bodies implemented in beta123.

- The architecture notes now list one-line `say`/`show`/`말해` bodies after
  `then`/`그러면` for native condition blocks and branches.
- The memo also records that Python inline bodies and inline value changes stay
  outside the restricted native subset.
