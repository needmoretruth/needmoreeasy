# NME 0.0.1-beta.59

beta.59에서는 실행되지 않는 분기의 네이티브 확정 바인딩 분석을 수정합니다.

- `if true` 뒤의 실행되지 않는 `else`나 `else if`에서만 만든 바인딩은 블록 밖에서
  초기화된 이름으로 남지 않습니다.
- 해당 분기는 네이티브 타입 호환성을 계속 검사하지만, 이후 읽기에는 올바르게
  초기화되지 않은 이름 진단을 제공합니다.
