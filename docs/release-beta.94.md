# NME 0.0.1-beta.94

Beta.94 aligns Korean operational guide examples with Korean CLI commands.

- Use Korean run/check commands in the file, high-score, HTTP, and terminal-menu
  workflows.
- Use the Korean native run/build commands in the native guide.
- Keep English commands where a section explicitly compares the two paths.

Language semantics and generated Python behavior are unchanged.
