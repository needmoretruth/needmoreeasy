# NME 0.0.1-beta.124

beta124에서는 beta123에 구현한 네이티브 한 줄 NME 제어문 본문을 네이티브
백엔드 설계 문서에도 반영합니다.

- 설계 문서가 네이티브 조건문과 분기에서 `then`/`그러면` 뒤의 한 줄
  `say`/`show`/`말해` 본문을 허용된 기능으로 기록합니다.
- Python 인라인 본문과 인라인 값 변경은 제한된 네이티브 부분집합 밖이라는
  경계도 함께 기록합니다.
