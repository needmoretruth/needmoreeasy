# NME 0.0.1-beta.33

beta.33에서는 모듈과 관련된 두 CLI 실패를 정확한 진단으로 구분합니다.

- 여러 파일의 모듈 이름 충돌을 옵션 값 오류로 표시하지 않고 E9028
  `가져온 모듈 두 개의 이름이 같습니다`로 표시합니다.
- 현재 네이티브 컴파일 제한에는 E9029를 추가하고
  `nme ko E9029`·`nme en E9029`에서 한영 설명을 제공합니다.
- 두 실패의 한영 CLI 회귀 검사를 추가하고 E9003은 옵션 값이 없거나 잘못된
  경우에만 사용하도록 유지합니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
