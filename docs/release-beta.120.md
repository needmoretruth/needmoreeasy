# NME 0.0.1-beta.120

Beta120 adds the missing regression coverage for Korean comparison endings
before the `또는`/`or` connector inside a parenthesized condition.

- Verify shared lowering of `만약 (점수가 2보다 작으면 또는 준비)`.
- Verify the same form through the restricted native backend.
