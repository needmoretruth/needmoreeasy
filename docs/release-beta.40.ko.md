# NME 0.0.1-beta.40

beta.40에서는 오래된 임시 준비 파일이 다음 실행에 영향을 주지 않게 합니다.

- 가져온 모듈, 네이티브, Nuitka 준비 단계가 충돌 검사를 포함한 실행별 새
  임시 폴더를 사용합니다.
- 중단 또는 프로세스 ID 재사용 뒤 프로세스 ID만으로 된 폴더를 재사용하지 않습니다.
- 오래된 Python 모듈이 다음 일반 import를 가리는지 확인하는 결정적인 Unix
  회귀 검사를 추가했습니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
