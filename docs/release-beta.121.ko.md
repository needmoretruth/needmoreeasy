# NME 0.0.1-beta.121

beta.121에서는 괄호 안의 한국어 `while` 비교 끝말이 논리 연결어보다 앞에
오는 경우를 고칩니다.

- `동안 (횟수가 2보다 작을 동안 그리고 준비)`에서 안쪽 `동안`을 반복 본문의
  경계로 잘못 읽지 않습니다.
- `그리고`/`또는`와 `and`/`or`가 같은 토큰·조건 경로를 사용하며 네이티브
  백엔드에서도 동작하는지 확인합니다.
