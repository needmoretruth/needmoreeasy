# NME 0.0.1-beta.160

Beta160 makes the pull-request Format gate fetch the parent commit required by
its changed-file diff. The scoped rustfmt check now works with the shallow
checkout used by the platform CI jobs.
