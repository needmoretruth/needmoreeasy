# NME 0.0.1-beta.158

beta158에서는 세미콜론으로 구분한 단순 문장 전체에 공유 Python 문맥 검사를
적용합니다. 한 줄 함수·클래스 본문에서 앞선 문장 뒤에 오는 별표 import에는
`E0114`를 표시하고, `except*` 핸들러에서 앞선 문장 뒤에 오는 `break`,
`continue`, `return`에는 `E0115`를 표시해 일반 CLI 진단으로 빠지지 않도록
합니다.
