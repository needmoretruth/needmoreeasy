# NME 0.0.1-beta.107

beta.107에서는 네이티브 조건부 바인딩의 분기 병합 수정을 완성합니다.

- 앞서 조건부였던 이름도 뒤의 `if`/`else` 모든 분기에서 구체적으로 대입할 때만
  블록 뒤에서 사용할 수 있습니다.
- 한쪽 분기와 실행되지 않을 수 있는 반복문은 계속 보수적으로 처리하며 영어·한국어
  회귀 테스트로 확인합니다.
