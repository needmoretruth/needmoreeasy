# NME 0.0.1-beta.35

beta.35에서는 가져온 모듈을 읽지 못할 때 파일 읽기 진단을 사용합니다.

- 읽을 수 없는 가져온 `.nme` 모듈을 최상위 프로그램이 없을 때 쓰는 E9015 대신
  E9007 `파일을 읽을 수 없습니다`로 표시합니다.
- 한영 E9007 설명을 넓히고, 없는 가져온 모듈을 검사하는 영어·한국어 CLI
  회귀 검사를 추가했습니다.

언어 의미와 생성되는 Python 동작은 바뀌지 않습니다.
