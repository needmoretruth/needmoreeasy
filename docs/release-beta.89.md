# NME 0.0.1-beta.89

Beta.89 improves the break-loop guide's language coverage.

- Use Korean output and break-loop spellings throughout the Korean example.
- Use English `while`, `ask`, `if`, `break`, and `end` in the English exercise.
- Retain a dedicated mixed-language example so the guide still teaches that
  English and Korean forms can coexist.

Language semantics and generated Python behavior are unchanged.
